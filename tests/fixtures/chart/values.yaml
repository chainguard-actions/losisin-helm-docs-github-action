# -- Number of replicas
replicaCount: 1

# -- Container image configuration
image:
  # -- Image repository
  repository: nginx
  # -- Image tag
  tag: "latest"
  # -- Image pull policy
  pullPolicy: IfNotPresent

# -- Service configuration
service:
  # -- Service type
  type: ClusterIP
  # -- Service port
  port: 80
